<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ErgastAPP.App">
    <Application.Resources>
        <ResourceDictionary>

            <!--<OnPlatform x:TypeArguments="x:String"
                        x:Key="BoldFont"
                        WPF="/Assets/exo2extrablod.ttf#Exo 2 Extra Bold"/>
            <OnPlatform x:TypeArguments="x:String"
                        x:Key="NormalFont"
                        Android="exo2medium.ttf#Exo 2 Medium"
                        iOS="exo2medium" />
            <OnPlatform x:TypeArguments="x:String"
                        x:Key="ItalicFont"
                        Android="exo2mediumitalic.ttf#Exo 2 Medium"
                        iOS="exo2mediumitalic" />-->

            <!--<OnPlatform x:TypeArguments="x:String" x:Key="BoldFont">
                <On Platform="Android" Value="exo2extrablod.ttf#Exo 2 Extra Bold" />
                <On Platform="UWP" Value="/Assets/exo2extrablod.ttf#Exo 2 Extra Bold" />
                <On Platform="iOS" Value="exo2extrablod" />
            </OnPlatform>-->
            <!--<OnPlatform x:TypeArguments="x:String" x:Key="NormalFont">
                <On Platform="Android" Value="exo2medium.ttf#Exo 2 Medium" />
                <On Platform="UWP" Value="/Assets/exo2medium.ttf#Exo 2 Medium" />
                <On Platform="iOS" Value="exo2medium" />
            </OnPlatform>-->
            <!--<OnPlatform x:TypeArguments="x:String" x:Key="ItalicFont">
                <On Platform="Android" Value="exo2mediumitalic.ttf#Exo 2 Medium" />
                <On Platform="UWP" Value="/Assets/exo2mediumitalic.ttf#Exo 2 Medium" />
                <On Platform="iOS" Value="exo2mediumitalic" />
            </OnPlatform>-->

            <!-- COLOR STYLES -->
            <Color x:Key="NavigationPrimary">#FF0000</Color>
            <Color x:Key="Primary">#FF0000</Color>
            <Color x:Key="Accent">#ff8b85</Color>
            <Color x:Key="LightTextColor">#999999</Color>

            <Color x:Key="BackgroundWhite">#FFFFFF</Color>
            <Color x:Key="BackgroundDark">#1c1c1c</Color>
            <Color x:Key="BackgroundLightDark">#454545</Color>
            
            <!-- NAVIGATIONPAGE -->
            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource NavigationPrimary}" />
                <Setter Property="BarTextColor" Value="White" />
            </Style>

            <!-- STACKLAYOUT STYES -->
            <Style TargetType="StackLayout"
                   x:Key="MainLayout">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource BackgroundDark}" />
                <Setter Property="Padding"
                        Value="10" />
            </Style>

            <Style TargetType="StackLayout"
                   x:Key="SectionLayout">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource BackgroundLightDark}" />
                <Setter Property="Margin"
                        Value="5,20,5,20" />
                <Setter Property="Padding"
                        Value="5" />
            </Style>

            <Style TargetType="StackLayout"
                   x:Key="MenuLayout">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="VerticalOptions"
                        Value="FillAndExpand" />
            </Style>

            <Style TargetType="StackLayout"
                   x:Key="TableLayout">
                <Setter Property="Margin"
                        Value="0" />
                <Setter Property="Padding"
                        Value="0" />
            </Style>

            <!-- LABEL STYLES -->

            <Style TargetType="Label">
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />
            </Style>

            <Style TargetType="Label"
                   x:Key="MenuItem">
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
                <Setter Property="VerticalTextAlignment"
                        Value="Center" />
            </Style>

            <Style TargetType="Label"
                   x:Key="SectionHeader">
                <Setter Property="LineBreakMode"
                        Value="WordWrap" />
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
                <Setter Property="BackgroundColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />
                <Setter Property="HorizontalOptions"
                        Value="Fill" />
                <Setter Property="HorizontalTextAlignment"
                        Value="Center" />
                <Setter Property="Margin"
                        Value="0,0,0,5" />
            </Style>

            <Style TargetType="Label"
                   x:Key="LabelListItemHeader">
                <Setter Property="LineBreakMode"
                        Value="NoWrap" />
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
            </Style>

            <Style TargetType="Label"
                   x:Key="LabelListItemDetails">
                <Setter Property="LineBreakMode"
                        Value="NoWrap" />
                <Setter Property="FontSize"
                        Value="Small" />
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />
            </Style>

            <Style TargetType="Label"
                   x:Key="Details_Header">
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
            </Style>

            <Style TargetType="Label"
                   x:Key="Details_MoreHeader">
                <Setter Property="FontSize"
                        Value="Small" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
            </Style>

            <Style TargetType="Label"
                   x:Key="Details_Info">
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="LineBreakMode"
                        Value="WordWrap" />
            </Style>

            <Style TargetType="Label"
                   x:Key="Details_MoreInfo">
                <Setter Property="FontSize"
                        Value="Small" />
                <Setter Property="LineBreakMode"
                        Value="MiddleTruncation" />
            </Style>

            <Style TargetType="Label"
                   x:Key="HeaderResult">
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="FontAttributes"
                        Value="Bold" />
                <Setter Property="BackgroundColor"
                        Value="#000000" />
                <Setter Property="Margin"
                        Value="0" />
                <Setter Property="HorizontalOptions"
                        Value="Fill" />
                <Setter Property="HorizontalTextAlignment"
                        Value="Center" />
                <Setter Property="VerticalOptions"
                        Value="Fill" />
            </Style>

            <Style TargetType="Label"
                   x:Key="ListItem">
                <Setter Property="Margin"
                        Value="2" />
                <Setter Property="HorizontalOptions"
                        Value="Fill" />
                <Setter Property="HorizontalTextAlignment"
                        Value="Center" />
            </Style>

            <!-- SEARCHBAR STYLES -->
            <Style TargetType="SearchBar">
                <Setter Property="HorizontalOptions"
                        Value="FillAndExpand" />
                <Setter Property="BackgroundColor"
                        Value="{StaticResource BackgroundLightDark}" />
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />

            </Style>
            
            <!-- PICKER STYLES -->

            <Style TargetType="Picker">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource BackgroundLightDark}" />
                <Setter Property="TextColor"
                        Value="{StaticResource BackgroundWhite}" />
                <Setter Property="HorizontalOptions"
                        Value="FillAndExpand" />
            </Style>
            

            <!-- BUTTON STYLES -->
            <Style TargetType="Button"
                   x:Key="ButtonMark">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="TextColor"
                        Value="#FFFFFF" />
                <Setter Property="VerticalOptions"
                        Value="CenterAndExpand" />
                <Setter Property="Margin"
                        Value="5" />
                <Setter Property="HorizontalOptions"
                        Value="Center" />
                <Setter Property="BorderColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="BorderRadius"
                        Value="5" />
                <Setter Property="BorderWidth"
                        Value="2" />
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
</Application>